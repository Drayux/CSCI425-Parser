const float pi = 3.14;
const int x = 42;
float silly()
returns r=0
{
    int i = 0, j = 1, k = 2;
    while( i < 3 + j + k ) {
        i = i + 1;
        emit ( pi );
        k = i = x = j;
    }
    x = i + x + int(pi);
    pi = 3.14159;
    r=silly();
}
